# 开发过程中的常见问题以及解决方法 (error.md)

## 1. 列表在大数据量下存在严重白屏与滚动不同步
**原因：**
使用 Vue 传统的 reactive 或者 ref 包裹十万级/百万级的超大数组（或包含了大量对象引用的数组）时，会在收集依赖时产生极大的性能开销，导致内存与 CPU 负载过高。此外，传统按项插入真实 DOM 的方式会导致布局 (Layout) 超时。

**解决方法：**
1. 采用 `shallowRef` 替代普通的 `ref` 来保存数据列与布局高度信息（Heights & Offsets）。
2. 将每个元素距顶部的距离剥离为单独的 Float64Array 等定型数组数据结构，不仅节省内存，且遍历时间极快。
3. 利用 `Array.prototype.slice` 或者基于二分查找的可视区计算方法替代 `v-if`/`v-show` 指令。

## 2. 列表元素高度不确定导致的滚动轴抖动 (Scrollbar Jitter)
**原因：**
当我们在向下滚动或者向上滚动时，由于没有滚动前预知每一个列表元素准确的身高，采用“边滚动边测量并调整滚动轴位置”策略必然会导致滚动总高度 (Total Height) 频繁变化，引起滚动条长短不停闪动，用户体验极差。

**解决方法：**
引入 `@chenglou/pretext` 库。这是一款纯粹的使用 Canvas 测量字体宽度但绝不触碰 DOM Reflow (重排) 的库。由于规避了 `getBoundingClientRect()` 调用，我们可以在极短时间内（百毫秒级）瞬间预计算出十万条数据中所有由文本排版撑开的高度，构建出“精确且永不抖动”的总虚拟滚动高度。

## 3. 监听滚动性能不足
**原因：**
滚动事件（`scroll`）触发频率极高，如果在里面做了大量的复杂运算或者修改响应式 DOM 数，往往导致页面丢帧与掉跟。

**解决方法：**
在主容器绑定 `@scroll.passive="handleScroll"` 确保滚动完全依靠浏览器子线程执行。不要把 DOM 逻辑挂载在 scroll 函数中，只负责记录一个简单的 `scrollTop`。
